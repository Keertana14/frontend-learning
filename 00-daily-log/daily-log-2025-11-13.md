# 🗓️ Daily Log — 13 Nov 2025

## 📚 What I Learned

### **HTML Fundamentals — Summary**
- Understood the difference between **regular tags** and **self-closing (void) tags** such as `<br>`, `<hr>`, `<img>`, `<input>`, etc.
- Learned how **images** work in HTML, focusing on `src` and `alt` attributes for accessibility and SEO.
- Studied **bitmap vs vector images**, including common formats like **JPEG**, **PNG**, and **SVG**.
- Practiced using **inline SVG** and using SVG files with the `<img>` tag.

### **HTML Forms**
- Learned how the `<form>` tag works with `action` and `method`.
- Explored various **input types** including text, email, number, checkbox, radio, file, etc.
- Understood the purpose of **labels** using matching `for` and `id` attributes.
- Worked with **textarea**, **dropdowns (select + option)**, and different **button types** (`submit`, `reset`, `button`).
- Understood behavior of **checkboxes vs radio buttons** and attributes like `checked`, `disabled`, and `value`.
- Learned the basics of **form validation** (client-side vs server-side).
- Understood what happens during **form submission** — encoding, sending request, server processing, and response.

### **HTML Structure & Semantics**
- Learned the difference between **block** and **inline** elements.
- Understood container elements like **div**, **section**, **article**, **header**, **footer**, **nav**, **main**, and **span**.
- Learned why **semantic HTML** improves accessibility, SEO, and readability.
- Explored important semantic tags like `header`, `main`, `article`, `aside`, `figure`, `figcaption`, `time`, `code`, and more.

### **Scientific & Tabular HTML**
- Learned how to build proper **HTML tables** using `table`, `tr`, `th`, `td`, along with `thead`, `tbody`, and `tfoot`.
- Studied how to merge cells using **colspan** and **rowspan**.
- Used **subscript** (`<sub>`) and **superscript** (`<sup>`) for scientific notation.
- Practiced using **figure + figcaption** for descriptive media with context.
- Understood the history of tables in early web design and why modern development uses **Flexbox/Grid** for layouts instead.